# CLAUDE.md — ngj_10

Game jam project. Unity 2D. Repo: `damarrus/ngj_10`.

## Философия

Геймджем, но **делаем сразу хорошо**. Писать код быстро ≠ писать говнокод — на одной скорости можно выдать и чистое, и кривое решение, так что берём чистое по умолчанию. Цель — рабочий билд в срок на коде, который не развалится и не запутает к середине джема.

- **Делаем сразу правильно.** Не оставляем костыли «потом перепишем» — потом не будет. Если правильное решение по объёму близко к кривому (а так почти всегда) — берём правильное. Упрощаем **только** при реальном супер-усложнении (десятки строк / новая абстракция ради мелочи).
- **Один путь на один эффект.** Один инпут/событие обрабатывается одним обработчиком. Не дублируем обработку (напр. ручной polling мыши + UI `onClick` на один клик — это и есть источник багов и костылей-гардов).
- **Связность через C# events / UnityEvent**, не через ручные перекрёстные ссылки. Владелец UI поднимает событие — потребитель подписывается. Виджеты наружу не торчат.
- **Не оверинженерить.** Никаких DI-контейнеров, event-bus-фреймворков, абстрактных фабрик, ECS, паттернов ради паттернов. Прямой код, который видно насквозь. C# events / UnityEvent — это не «event-bus», это норма.
- **Не говнокодить.** Понятные имена, мелкие методы, идемпотентные `Pause/Resume/Toggle`-операции, никаких `Update()` на 200 строк, magic numbers геймплея → `[SerializeField]`. Код читается без расшифровки.
- **Никаких маскирующих костылей.** Frame-guard / флаги «уже сделал в этом кадре» / `yield return null` чтобы «обойти» порядок — это симптом двойной обработки. Чинить корень, не симптом.
- **YAGNI.** Не пишем под «вдруг понадобится». Только то, что нужно сейчас — но то, что нужно, пишем чисто.
- **Дублирование терпимо** в малых дозах (2 раза). Третий раз — рефакторим. Не плодим абстракцию из-за двух похожих кусков.

## Тех-стек

- Unity **6000.4.9f1**, 2D (URP), Universal Render Pipeline 17.4
- **Целевая платформа — WebGL** (веб-билд в первую очередь). Всё писать с оглядкой на браузер.
- Input System (new) + legacy (Active Input Handling = **Both**)
- C#, namespace `Ngj10` (asmdef `Ngj10` в `Assets/_Project/Scripts/`)

## WebGL — главный таргет

Игра в первую очередь для веб-билда. Ограничения браузера — учитывать всегда:

- **Нет многопоточности по умолчанию.** Не использовать `System.Threading.Thread`, тяжёлый `Task.Run`. Корутины / async-await на main thread — ок.
- **Файловая система ограничена.** Нет прямого `System.IO` к произвольным путям. Сохранения — через `PlayerPrefs` (он мапится в IndexedDB) или `Application.persistentDataPath`. Не читать/писать произвольные файлы.
- **Звук:** WebAudio. Первый звук может требовать user-gesture (клик) — браузеры блокируют автоплей. Не рассчитывать на звук до первого инпута.
- **Размер билда критичен** — грузится по сети. Не тащить тяжёлые пакеты, большие текстуры, лишние зависимости. Сжимать спрайты.
- **Производительность:** WebGL медленнее нативного. Минимум аллокаций в `Update`, не спавнить тысячи объектов, пулить если много.
- **Нет:** рефлексии-хеви кода (IL2CPP стрипает), `Process`, серверных сокетов. Сеть — только `UnityWebRequest` / fetch.
- **Ввод:** мышь + клавиатура + тач. Геймпад в браузере ненадёжен — не основной.
- **Курсор/фуллскрин** — по user-gesture, не программно в любой момент.

Билд: `File > Build Profiles`, платформа **Web**. Проверять в браузере, не только в Editor (поведение отличается).

## Структура

```
Assets/_Project/
  Scenes/      Boot, Game, End  (build order: Boot=0 -> Game=1 -> End=2)
  Scripts/
    Core/      GameManager, SceneLoader, BootStarter, GameState, SceneNames
    Gameplay/  игровая логика
  Prefabs/  Art/{Sprites,UI}  Audio/{Music,SFX}  Materials/  Settings/
```

Весь новый контент — под `Assets/_Project/`. Не сорить в корень `Assets/`.

## Prefab-first — игровые объекты в prefab, не в сцену

**Игровые объекты делаем prefab'ами, на сцену кладём инстанс.** Причина — merge-конфликты: `.unity` — один огромный YAML с fileID-связями, двое правят сцену → конфликт на сотнях строк, ручной мёрж почти нереален. Prefab — отдельный файл, правки изолированы; инстанс на сцене хранит только overrides. Делёж работы (один — prefab игрока, другой — prefab врага) = разные файлы = 0 конфликтов.

- **В prefab:** всё, что переиспользуется (игрок, враги, пули, пикапы, UI-виджеты-строки), и всё, что часто меняется кодом/MCP. Правка идёт в `.prefab`, не в сцену.
- **На сцене ок:** уникальный для одной сцены синглтон-объект, который никто не трогает параллельно (Main Camera, конкретный спавнер уровня, корень UI-canvas). Не оверинженерить — не каждый объект обязан быть prefab'ом.
- **Правка prefab — через `assets-prefab-open` → правки → `assets-prefab-save`/`close`** (MCP), не редактируя `.prefab` YAML руками.
- UI-вёрстку всё равно делаем на сцене руками (см. `docs/ui-conventions.md`) — но переиспользуемые куски (строка лидерборда, кнопка-шаблон) выносим в prefab.

## Поток сцен

- **Boot** — точка входа. `BootStarter` создаёт `GameManager` (persistent), авто-грузит Game.
- **Game** — геймплей.
- **End** — экран конца, рестарт по любой клавише.
- Переходы только через `SceneLoader` / `GameManager`. Имена сцен — в `SceneNames`, не хардкодить строки.

## Код-конвенции

- `PascalCase` классы/методы/публичные поля, `_camelCase` приватные поля, `[SerializeField] private` для инспектора.
- Один класс — один файл, имя файла = имя класса.
- Namespace по папке: `Ngj10.Core`, `Ngj10.Gameplay`.
- Комментарии — только где «почему», не «что». Очевидное не комментировать.
- Magic numbers геймплея → `[SerializeField]` поля, чтобы тюнить в инспекторе без рекомпиляции.

## UI-текст — только TextMeshPro

- **Весь UI-текст — `TextMeshProUGUI` (TMP). Legacy `UnityEngine.UI.Text` не использовать.** TMP резче на любом масштабе (SDF), стабильнее авто-сайз, богаче (rich text). В Unity 6 TMP уже внутри `com.unity.ugui` — отдельный пакет не нужен.
- Шрифт-ассет: дефолтный `LiberationSans SDF` (TMP Essential Resources, лежит в `Assets/TextMesh Pro/`). При билде UI кодом TMP сам подхватит дефолтный font asset из `TMP_Settings` — явно ссылку можно не вешать.
- Если код строит UI динамически: `go.AddComponent<TextMeshProUGUI>()`, `using TMPro;`. Размеры/выравнивание — `fontSize`, `alignment = TextAlignmentOptions.*`.

## Unity MCP — как я работаю с Editor

Подключён мост **IvanMurzak/Unity-MCP** (skill-based tools: `scene-*`, `gameobject-*`, `assets-*`, `console-*`). Работает **без кликов** — фокус-throttle нет, MCP-запросы не требуют переключения фокуса на Unity.

- **`.unity`/`.prefab`/`.asset` не редактировать руками** — сложный YAML + GUID-связи. Только через MCP.
- **Скрипты, ProjectSettings, JSON, .md** — можно править файлами напрямую.
- **Ссылки (refs) РАБОТАЮТ** — wiring через `gameobject-component-modify`, value `{"instanceID":N}`. Это была причина переезда со старого моста (там wiring был сломан). Code-driven (`GetComponent`/`transform.Find`/`FindAnyObjectByType`) теперь не обязателен, но валиден.
- **Рекомпиляция:** `assets-refresh` форсит компиляцию (ждёт результата через requestId). После правки `.cs` файлами напрямую — вызвать `assets-refresh`.
- После правок проверять консоль (`console-get-logs`, errors) на ошибки компиляции.
- **Unity 6 Find API:** `FindAnyObjectByType<T>()` (и `FindObjectOfType`, и `FindFirstObjectByType` — deprecated).
- Play Mode — через `editor-application-set-state` (или руками оператором).
- Полные грабли и приёмы по MCP → `docs/unity-mcp-notes.md`. Дополнять при новых открытиях.

## Git

- Коммитить/пушить — только по просьбе оператора.
- **Перед коммитом, затрагивающим сцену/префаб/ассет — проверить, что сцена сохранена.** Открытая сцена может быть `IsDirty` (несохранённые правки живут в памяти редактора, не на диске). Проверка: `scene-list-opened` → если `IsDirty: true`, сохранить через `scene-save` (или попросить оператора сохранить), иначе закоммитишь старое состояние файла.
- Без `Co-Authored-By`. Автор — разработчик.
- `Library/`, `Temp/`, `.sln`, `*.csproj` игнорятся (см. `.gitignore`).
- `.meta` файлы коммитить всегда вместе с ассетом.
- `.mcp.json` содержит абсолютный путь с хэшем PackageCache — локально-специфичен, у тиммейтов путь другой.

## Скиллы и доки эффективности (читать по задаче)

Проектные скиллы (`.claude/skills/`, авто-триггер по описанию):
- **`unity-ui`** — постройка/итерация UI (RectTransform, anchoring, layout, TMP,
  prefab-template, screenshot-loop). Триггер: правка UI/RectTransform/LayoutGroup.
- **`unity-verify`** — проверка после правок (L1 compile → L2 play → L3 screenshot,
  read-back после wiring: success ≠ applied). Триггер: после `.cs`/scene/wiring правок.

Доки (справочники, читать когда задача в теме):
- `docs/ui-conventions.md` — **source of truth по UI** (выстрадано на лидерборде):
  UI вёрстают на сцене руками, код = логика; ScrollRect/маска грабли; скрин только
  Play/оператор. `unity-ui` скилл ссылается сюда.
- `docs/unity-efficiency.md` — lifecycle-ловушки, domain-reload static-state, WebGL
  gotchas, perf-гигиена, script-execute vs file, архитектура под jam.
- `docs/unity-mcp-notes.md` — механика моста, грабли формата запросов, allowlist,
  Play-фокус, мёртвый Edit-mode скрин.
- `docs/leaderboard.md` — устройство лидерборда (Supabase REST, uid, RLS, REST-детали).

## Не делать

- Не читать файл повторно после Edit/Write для проверки.
- Не плодить менеджеры/синглтоны без нужды. `GameManager` — один, остальное по месту.
- Не тащить пакеты из Asset Store / NuGet без согласования.
- Не рефакторить «заодно» то, о чём не просили — на джеме время дорого.
