namespace Ngj10.Core
{
    /// <summary>
    /// Central list of scene names. Keep in sync with the scenes added to
    /// File > Build Profiles (Scene List). Avoids magic strings across the codebase.
    /// </summary>
    public static class SceneNames
    {
        public const string Boot = "Boot";
        public const string Game = "Game";
        public const string End = "End";
    }
}
